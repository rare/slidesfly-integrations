name: Publish deck

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - id: deck
        uses: rare/slidesfly-integrations/actions/publish@2f4a9d00bb9e9347623fbd8600eaa967b250ea33 # v0.1.1
        with:
          file: path/to/deck.html
          api-key: ${{ secrets.SLIDESFLY_API_KEY }}
          title: Workflow deck
          visibility: unlisted
      - name: Show reader URL
        env:
          SLIDESFLY_READER_URL: ${{ steps.deck.outputs.url }}
        run: echo "$SLIDESFLY_READER_URL"
